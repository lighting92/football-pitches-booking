@model FootballPitchesBooking.Models.RecommendationModels.RecommendationModel
@{
    ViewBag.Title = "ListPromotedStadiums";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ListPromotedStadiums</h2>
<div class="row">
    <div class="span12">
        @if (Model.PromotionStadiums != null)
        {
            if (Model.PromotionStadiums.Count > 0)
            {
            <div class="row no-margin">
                <table class="table table-condensed table-bordered table-hover">
                    <thead>
                        <tr>
                            <th style="width: 150px;"></th>
                            <th style="width: 150px;">TÊN SÂN BÓNG</th>
                            <th>ĐỊA CHỈ</th>
                            <th style="width: 110px;">SỐ ĐIỆN THOẠI</th>
                            <th style="width: 90px;"></th>
                            <th style="width: 70px;"></th>
                        </tr>
                        <tr>
                            <th style="width: 150px;">HÌNH ẢNH</th>
                            <th style="width: 150px;">TÊN SÂN BÓNG</th>
                            <th>ĐỊA CHỈ</th>
                            <th style="width: 110px;">SỐ ĐIỆN THOẠI</th>
                            <th style="width: 90px;">KHUYẾN MÃI (%)</th>
                            <th style="width: 70px;"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @for (int i = 0; i < Model.PromotionStadiums.Count; i++)
                        {
                            <tr>
                                <td>
                                    @if (Model.PromotionStadiumsImages[i] != null)
                                    {
                                        <img src="/Content/images/@Model.PromotionStadiumsImages[i].Path" style="width: 150px; height: 70px;"/>
                                    }
                                    else
                                    {
                                        <img src="/Content/images/stadium.jpg" />
                                    }
                                </td>
                                <td><a href="/Stadium/Details/@Model.PromotionStadiums[i].Stadium.Id">@Model.PromotionStadiums[i].Stadium.Name</a></td>
                                <td>@Model.PromotionStadiums[i].Stadium.Street, @Model.PromotionStadiums[i].Stadium.Ward, @Model.PromotionStadiums[i].Stadium.District</td>
                                <td>@Model.PromotionStadiums[i].Stadium.Phone</td>
                                <td>    
                                    @if (Model.PromotionStadiums[i].HighestPromotion != null)
                        {                        
                            <p>Giảm giá @Model.PromotionStadiums[i].HighestPromotion.Discount%</p>                        
                        }                                
                                </td>
                                <td><a href="~/Stadium/Book?Stadium=@Model.PromotionStadiums[i].Stadium.Id">
                                    <button class="btn-small btn-success">Đặt sân</button></a></td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                    </tfoot>
                </table>
            </div>
            }
            else
            {
            <p>Không có sân bóng nào được tìm thấy.</p>
            }
        }
        else if (Model == null)
        {
            <p>Hiện tại không có sân bóng nào trên hệ thống.</p>
        }
    </div>
</div>
<script type="text/javascript">
    $(document).ready(function () {
        $('table').dataTable({
            aoColumnDefs: [
              {
                  bSortable: false,
                  aTargets: [-1, -2, -6]
              }
            ],
            "aaSorting": []
        }).columnFilter({
            sPlaceHolder: "head:after",
            aoColumns: [
                null,
                { type: "text" },
                { type: "text" },
                { type: "text" },
                null,
                null
            ]
        });
    });
</script>